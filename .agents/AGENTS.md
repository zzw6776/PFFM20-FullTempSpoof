# Workspace Rules

- 运行耗时较长的构建、测试或实机脚本时，必须打开用户可见的 macOS Terminal 窗口展示命令和实时输出；如果脚本已经在后台启动，则只在可见 Terminal 中跟随同一份日志，禁止重复启动脚本。
- 等待上述脚本期间，助手不得持续轮询进程或日志。应暂停操作并等待用户告知脚本已经结束，再读取一次最终输出并继续处理。
- 打开可见 Terminal 不会扩大操作授权；安装、刷写、应用补丁、重启等动作仍必须严格遵守用户明确给出的范围。
